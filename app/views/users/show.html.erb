<% provide(:title, @user.nomeCompleto) %>
<% if logged_in? %>

<div class="row">
  <div class="col-md-3"></div>
  <div class="col-md-8">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user, size: 200 %>
        Nome: <%= @user.nomeCompleto %></br>
        Login: <%= @user.login %>
      </h1>
      <p>Email: <%= @user.email %></p>      
      <p> Cidade: <%= @user.cidade %></p>
      <p> Estado: <%= @user.estado %></p>
      <p> Descrição: <%= @user.descricao %></p>
    </section>
  </div> 
</div></br>
<div class="row">
	<div class="col-md-5"></div>
	<div class="col-md-3">
		<% if !current_user.admin? && current_user?(@user) %>
		<%= link_to "Apagar perfil", @user.destroy,
		  data: { confirm: "Você esta certo disso?" }, class: "btn btn-primary" %> 
		<%=log_out%>
    <% end %>  

	</div>
</div>	</br>
<div class="row">
	<div class="col-md-5"></div>
	<div class="col-md-5">
	<ul class="users">
	    <%= for i in (0..5) do %>
	    <li>
	      <%= ind = Random.rand(User.all.size); %>
	      <%= gravatar_for User.all[ind], size: 50 %>
	      <%= link_to User.all[ind].nomeCompleto, User.all[ind] %>
	    </li>
	    <% end %>
	</ul>
	</div>
</div>

<% else %>
  	<div class="col-md-4"></div>
  	<div class="col-md-4">
  		<h3>Faça seu login para ter acesso a essas informações</h3></br>
  		<p>Essas informaçoes são apenas para consulta de usuários, se você já usuário
  			faça seu login, senão cadastre a proveite o que temos de melhor</p> 
    </div>     
<% end %>  

<footer>
  <p>Trabalho de Tópicos Especiais em Computação - Anuário 2014/2</p>
</footer>         